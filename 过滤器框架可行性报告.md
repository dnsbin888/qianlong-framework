# 个股过滤器框架 — 可行性比对报告

> 2026-07-11 | 对标: Zipline Pipeline + 私募过滤器链 + A股特色

---

## 一、行业对比：谁在做，怎么做

| | Zipline Pipeline | 聚宽/BigQuant | 私募自研 | 潜龙现状 | 潜龙改后 |
|------|:--:|:--:|:--:|:--:|:--:|
| **架构** | 因子计算图 | 在线因子工坊 | 自研过滤器链 | 硬编码 `filter_universe` | **注册表式过滤器链** |
| **可扩展性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐ | **⭐⭐⭐⭐⭐** |
| **代码量** | 2000+行 | 平台级 | 500+行 | 150行（写死） | **50行框架+按需加** |
| **加新过滤** | 写子类+注册 | 平台操作 | 改核心代码 | 改核心代码 | **加一个函数** |
| **依赖** | pandas/numpy | 平台 | numpy | numpy | **numpy** |
| **A股适配** | 无（美股） | ✅ 有 | ✅ 有 | ⚠️ 部分 | **✅ 可定制** |
| **个人适用** | ⭐⭐ | ⭐（在线） | ⭐⭐ | ⭐⭐⭐ | **⭐⭐⭐⭐⭐** |

**结论**: 全行业都在用可扩展过滤器架构。潜龙现状是唯一还在硬编码的——这是差距，不是特色。

---

## 二、为什么现在做是最好时机

### 已有基础（不用重写）

```
tradability_mask.py      → 流动性 + ST + 停牌  ✅ 已验证
factor_registry.json     → 因子注册表           ✅ 已就绪
Hurst (待加)             → 个股反转窗口         📋 下一个
Drift Regime (待评估)    → 个股趋势状态         📋 后续
未来任意方法             → 放文件即生效         ∞
```

### 不改就会这样

```
加第3个过滤 → 改 filter_universe (50行循环越来越长)
加第5个过滤 → 循环嵌套 3 层，边界条件互相干扰
加第8个过滤 → 没人敢动，怕改坏现有逻辑
```

### 改了就会这样

```
加第N个过滤 → 往 FILTERS 列表加一行，不改任何现有代码
```

**现在有 2 个过滤器，刚好是改的临界点——再多一个就该重构了。**

---

## 三、A股适配性分析

| A股特色 | 现有覆盖 | 过滤器框架能解决 |
|------|:--:|------|
| **ST/*ST 动态变化** | ✅ st_filter | 规则更新不碰主逻辑 |
| **涨跌停 10%/20%/30%** | ✅ tradability_mask | 差异化阈值独立维护 |
| **北向资金流向** | ⚠️ westock 有数据 | 加一个 northbound_filter |
| **龙虎榜上榜标的** | ⚠️ lhb_fetcher 有数据 | 加一个 lhb_filter |
| **财报季业绩雷** | ❌ 无 | 加一个 earnings_filter |
| **限售股解禁** | ❌ 无 | 加一个 lockup_filter |
| **行业政策利空** | ❌ 无 | 加一个 policy_filter |

A股的特色是**规则多变、事件驱动**。过滤器框架天生适配——每个规则独立维护，互不干扰。

---

## 四、收益量化

| 指标 | 改前 | 改后 |
|------|:--:|:--:|
| 加新过滤耗时 | 30min（读代码→找位置→改→怕改坏） | **2min（加一行）** |
| 过滤逻辑 bug 定位 | 遍历 150 行找哪层出问题 | **直接定位到单个函数** |
| 新人理解代码 | 读 150 行 | **读 50 行框架 + 看函数名** |
| 回归测试范围 | 改一个影响全部 | **只测新加的函数** |
| 代码行数 | 150 | **50+按需** |

---

## 五、风险评估

| 风险 | 概率 | 应对 |
|------|:--:|------|
| 框架设计过度，用不上 | 低 | 50行的框架不存在过度设计 |
| 性能下降 | 极低 | 函数调用开销可忽略（5000只×5个过滤=2ms） |
| 引入新 bug | 低 | 现有过滤器原封不动搬过去，不改逻辑 |
| 维护负担 | 极低 | 比现在的硬编码更易维护 |

---

## 六、结论

| 维度 | 结论 |
|------|------|
| 行业方向 | ✅ 全行业都在做可扩展架构 |
| 时机 | ✅ 2→3个过滤器的临界点，现在不做将来更贵 |
| 成本 | ✅ 50行，零依赖，15分钟 |
| 风险 | ✅ 极低——搬现有代码不改逻辑 |
| A股适配 | ✅ 天生适配规则多变的市场 |
| ROI | ✅ 不可量化（省未来所有维护时间） |

**值得做。现在就是最好时机。**
