# 任务卡 — 六层漏斗补全

> 创建: 2026-07-09 | 优先级: 🟡 P1 (7项) | 状态: 待审批

---

## 背景

对标私募标准六层漏斗，潜龙当前综合完成度约 74%。识别出 7 项可补缺口，均为保护性规则——不碰模型、不影响现有信号逻辑、零新依赖。

---

## 当前完成度

```
第0层   ████████░░  80%  缺停牌检测/净资产为负
第0.5层 ██░░░░░░░░  20%  缺流动性过滤+缓冲区（整层空白）
第1层   █████████░  90%  缺负向剔除
第2层   ████████░░  85%  缺PSI特征稳定性/牛熊分测
第3层   ██████░░░░  60%  中性化码已写未接入/缺信号衰减
第4层   ████████░░  80%  缺峰值回撤熔断/持仓相关性
第5层   ████████░░  80%  方案A够用
第6层   █████████░  90%  达标
              ────
综合         74%
```

---

## 任务清单

### 🔴 P0 — 今天做（3项，纯规则，零风险，3h）

#### 任务 1：第 0.5 层 — 流动性过滤

**为什么重要**：成交量几百万的票，进去了出不来。赚了也白赚。这是整个漏斗最基础的保护，目前完全空白。

| 条件 | 阈值 | 动作 |
|------|------|------|
| 日均成交额 | < 1000 万 | 排除 |
| 近 20 日换手率 | < 0.5% | 排除（僵尸股） |
| 缓冲区 | 连续 20 日不满足才剔除 / 连续 30 日满足才纳入 | 避免股票频繁进出池子 |

**改动文件**: `tradability_mask.py`
**估时**: 1.5h
**风险**: 零。纯静态规则，不过滤反而多几个候选，不影响现有结果。

#### 任务 2：第 0 层 — 补硬过滤缺口

**为什么重要**：停牌股资金锁死，净资产为负随时变 ST。

| 条件 | 动作 |
|------|------|
| 长期停牌（>5 交易日） | 从候选池排除 |
| 净资产为负（财报） | 排除 |

**改动文件**: `tradability_mask.py`
**估时**: 0.5h
**风险**: 零。

#### 任务 3：第 4 层 — 峰值回撤熔断

**为什么重要**：目前只防单日暴跌（-5%），不防累计小亏。连亏 5 天每天 -2%，亏掉 10% 没有任何拦截。累计回撤才是真正的账户保护。

| 条件 | 动作 |
|------|------|
| 峰值回撤 ≥ 15% | 全停，人工复位 |

**改动文件**: `auto_breaker.py`（已有基础，加一条检查）
**估时**: 1h
**风险**: 零。只停不开，不影响已有持仓。

---

### 🟡 P1 — 本周做（3项，码已写或中等难度，7h）

#### 任务 4：第 3 层 — 行业中性化接入

**为什么重要**：不做中性化 = 你以为在选股，其实在押行业。新能源热的时候 ML 全推新能源，板块一跌全灭。

**行业标准做法**：
```
行业内 Z-score = (股票因子值 - 行业均值) / 行业标准差
```
每只股票的得分跟同行业比，不是跟全市场比。

**改动**: `exposure.py` 代码已写 → 接入 `generate_signal_table.py` 信号生成流程
**估时**: 2h
**风险**: 低。只改变排名相对位置，不改变模型。

#### 任务 5：第 3 层 — 市值中性化接入

**为什么重要**：size_score 权重 36%。大票天然高分，小票再优秀排不上号。截面回归去市值偏差后，小票好票才有机会。

**做法**：对 ML 得分做市值对数回归，取残差作为中性化后的得分。

**改动**: 新增截面回归模块 → 接入信号表
**估时**: 2h
**风险**: 低。

#### 任务 6：第 2 层 — PSI 特征稳定性监控

**为什么重要**：市场风格变了（比如从成长切价值），但模型不知道，还在用过去的规律推票。特征漂移了得告警。

**做法**：
```
训练时: 存每个特征的分布（分箱）
预测时: 计算 PSI = Σ (实际% - 预期%) × ln(实际%/预期%)
PSI > 0.1 → 告警：特征漂移，考虑重训
```

**改动**: 训练脚本存分布 → `pre_market_check.py` 增加第 12 项检查
**估时**: 3h
**风险**: 零。只监控不干预。

---

### 🟢 P2 — 按需做（1项，需数据验证，3h）

#### 任务 7：第 4 层 — 持仓相关性限制

**为什么重要**：3 只票同是新能源+大盘成长，跌一起跌。没有分散就没有风控。

**做法**：开仓前检查新票与已有持仓的行业/市值风格重叠度。超过阈值拒绝。

**改动**: `risk_guard.py` 加检查项
**估时**: 3h
**依赖**: 需先完成行业中性化（任务 4），否则没有行业分类可用。
**风险**: 可能过度限制导致买不进去。先做数据验证。

---

## 依赖关系

```
任务1(流动性) ──→ 任务4(行业中性化) ──→ 任务7(相关性限制)
任务2(硬过滤)    任务5(市值中性化)
任务3(回撤熔断)  任务6(PSI监控)

P0 三项独立，可并行开工
P1 三项独立，可并行开工
P2 依赖 P1 完成
```

---

## 做完后预期

```
第0层   80% → 90%  补停牌+净资产
第0.5层 20% → 90%  补流动性+缓冲区（最大提升）
第1层   90% → 90%  日线到头
第2层   85% → 90%  PSI监控
第3层   60% → 85%  中性化接入
第4层   80% → 90%  回撤熔断+相关性
第5层   80% → 80%  够用
第6层   90% → 90%  达标
              ────
综合         88%   (+14%)
```

---

## 验收标准

- [ ] 日成交额 < 1000 万的股票不出现在信号表
- [ ] 换手率 < 0.5% 的僵尸股不出现在信号表
- [ ] 停牌股不出现在候选池
- [ ] 峰值回撤 15% 触发后，总闸自动拉红
- [ ] 行业中性化后，信号不再集中在 1-2 个热门行业
- [ ] 市值中性化后，小票有机会排进前列
- [ ] PSI > 0.1 时，盘前检查有告警
- [ ] 全链路回归：`generate_signal_table.py` → `signal_table.json` → 首页信号卡片正常

---

## 关联

- [[选股到交易完整链路_六层漏斗分析]] — 完整对标文档
- [[architecture-upgrade-guide]] v4.0 — 三层对标体系 + 铁律#16 六层安检
- [[task-master-20260707]] — 任务总书
- [[system-assessment-20260707]] — 系统评估 v7.8
