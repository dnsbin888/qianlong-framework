# 模型组合优化蓝图 v1.0

**日期**: 2026-07-19  
**背景**: 共识率0%, 因子池不统一, 等权平均落后行业  
**方法**: 抄行业主流作业, 适配潜龙约束

---

## 一、行业主流做法速查

### 游资线 (打板+短线)
```
因子池: 六维动态权重 (动能/封单/人气/形态/资金/题材)
模型:   1个GBDT评分器 + HMM市场状态切换
输出:   Sigmoid → 0-100概率 → S/A/B分级 → 仓位映射
共识:   不需要 (单模型+多因子)
来源:   ULTIMATE-FUSION V12 / 雪球游资量化
```

### 中小私募线 (选股+组合)
```
因子池: 统一因子池 (所有模型共享)
模型:   LGBM + XGBoost + 1个线性校准器
融合:   Meta Model (逻辑回归学权重) ← 关键差异
训练:   滚动窗口 Walk-Forward
输出:   CSRank百分位 → Meta融合 → combined_score
来源:   广发金工2025 / DualAlpha-Lite
```

### 前沿线 (机构)
```
因子池: 300+ 因子 + FactorVAE自动生成
模型:   Transformer + GRU + GBDT + RL
融合:   多层Stacking + 注意力机制
训练:   多GPU并行
输出:   端到端交易决策
来源:   头部量化私募
```

---

## 二、适配潜龙的分析

### 硬约束
```
✅ CPU only (无GPU)
✅ ≤50因子
✅ ≤8持仓
✅ 日线级别
✅ pip install 可部署
✅ 个人账户规模
```

### 能抄什么

| 来源 | 能抄 | 不能抄 |
|------|------|--------|
| 游资 | 六维因子体系、动态权重、HMM | 手工盯盘部分 |
| 私募 | 统一因子池、Meta融合、WF训练 | GRU/Transformer(GPU) |
| 前沿 | 无 | 全部(GPU+大数据) |

**结论: 抄私募线——统一因子池 + GBDT双模型 + Meta加权融合。**

---

## 三、目标架构

```
                    ┌─────────────────────┐
                    │  统一因子池 (8因子)   │ ← 同一套因子, 所有模型共享
                    └─────────┬───────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │  LGBM    │  │ XGBoost  │  │  Ridge   │
        │ 排序回归  │  │ 分类     │  │ 线性校准  │
        └────┬─────┘  └────┬─────┘  └────┬─────┘
             │              │              │
             ▼              ▼              ▼
        CSRank%        CSRank%         CSRank%
         (0-1)          (0-1)           (0-1)
             │              │              │
             └──────────────┼──────────────┘
                            ▼
                   ┌─────────────────┐
                   │   Meta 融合层    │ ← 逻辑回归学权重 (替代等权)
                   │ W1·L+W2·X+W3·R  │
                   └────────┬────────┘
                            ▼
                   ┌─────────────────┐
                   │  combined_score │ → auto_trade_plan
                   │  共识: ≥2/3     │
                   └─────────────────┘
```

## 四、vs 现状对比

| 维度 | 现状 (问题) | 目标 (行业标准) |
|------|-----------|--------------|
| 因子池 | LGBM=63个, XGB=8个 (**不统一**) | 统一8因子 |
| 标签 | LGBM=CSRank, XGB=二分类 (**不一致**) | 统一CSRank(广发标准) |
| 输出 | 原始分数 (区间不一致) | CSRank百分位 (统一0-1) |
| 融合 | 等权平均 | Meta Model (逻辑回归) |
| 训练 | 单次+混合CV | 滚动Walk-Forward |
| 共识 | 0% (**致命**) | 预计60-80% |

## 五、实施路线

### Phase 1: 救命 (今天, 30分钟)
```
目标: 修复共识率=0% 的致命问题
做法: LGBM和XGB统一用8因子+CSRank标签重训
效果: 共识率预计恢复到 60-80%
```

### Phase 2: Meta融合 (本周, 2小时)
```
目标: 等权平均 → Meta Model加权
做法: 
  ① 每天截面: LGBM% + XGB% + Ridge% = 3个特征
  ② 逻辑回归学权重: W_lgbm, W_xgb, W_ridge
  ③ 每月重训一次Meta (轻量, 不需要重训底层模型)
效果: 融合精度提升, 自动适应市场切换
```

### Phase 3: 滚动训练 (下周, 4小时)
```
目标: 一次性训练 → 月度滚动 Walk-Forward
做法:
  ① 每月取过去3年数据重训
  ② Purged Walk-Forward CV
  ③ 自动检测模型退化→触发重训
效果: 防止过拟合, 适应市场变化
```

### Phase 4: 市场自适应权重 (择机)
```
目标: Meta权重 × 市场状态(regime)
做法:
  ① 牛市: XGB权重↑ (趋势跟踪强)
  ② 熊市: LGBM权重↑ (排序稳定)
  ③ 震荡: Ridge权重↑ (保守线性)
效果: 动态适配, 游资HMM思路的简化版
```

## 六、模型组合定案

| 模型 | 角色 | 权重来源 | 保留 |
|------|------|------|:--:|
| **LGBM** | 主力排序 | CSRank百分位 | ✅ |
| **XGBoost** | 互补验证 | CSRank百分位 | ✅ |
| **Ridge** | 线性仲裁 | CSRank百分位 | ✅ |
| **Meta LR** | 🆕 智能融合 | 逻辑回归学 | 🆕 |
| CatBoost | — | 保持禁用 | ❌ |

### CatBoost 的归宿

```
当前:    被禁用 (正确)
将来:    升级为 Meta 裁判
定位:    不参与选股, 只判断 "此刻该信LGBM还是XGB"
        = 市场状态感知器 + 模型选择器
触发:    Phase 4 市场自适应时重新引入
```

---

## 七、与行业对标

| 指标 | 游资 | 中小私募 | 前沿 | **潜龙Phase 2** |
|------|:--:|:--:|:--:|:--:|
| 模型数 | 1-2 | 2-4 | 4+ | **3+1(Meta)** |
| 因子池 | 6维自制 | 统一池 | 300+ | **统一8因子** |
| 融合 | 加权 | **Meta LR** | Stacking | **Meta LR** ✅ |
| CV | 无 | WF | PurgedWF | **PurgedWF** ✅ |
| 市场自适应 | HMM | 滚动训练 | 自适应 | **Phase 4** |
| 打板 | 专用模型 | 独立策略 | RL | **独立策略** ✅ |
| 过拟合审计 | 无 | DSR | DSR | **DSR** ✅ |

Phase 2 完成后, 潜龙模型体系 = 中小私募标准。Phase 4 完成后逼近前沿。

---

## 八、决策点

| # | 决策 | 建议 | 
|:--:|------|------|
| 1 | 因子池统一为8因子? | ✅ 是 (私募标准做法) |
| 2 | 标签统一为CSRank? | ✅ 是 (广发金工标准) |
| 3 | Meta融合替代等权? | ✅ 是 (最大收益项) |
| 4 | CatBoost保持禁用? | ✅ 是 (Phase 4以Meta身份回归) |
| 5 | Phase 1 现在做? | ⬜ 等你批 |
