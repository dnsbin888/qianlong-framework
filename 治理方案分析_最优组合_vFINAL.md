# 治理方案深度分析 + 最优组合 vFINAL

**日期**: 2026-07-19
**方法**: 治理文件×行业数据×实测发现 → 问题挖掘 → 最优结论

---

## 一、治理方案拆解

### 治理说了什么

```
定盘星 (2026-07-12):
  "干净2模型秩平均 + 诚实评测, 不追前沿ML"
  ML选股: ⭐⭐ 只做便宜的·封顶

信号铁律 (2026-07-15):
  ✅ CSRank标签  ✅ 原始分选股  ✅ per-model min-max归一化
  ❌ 禁止行业中性化  ❌ 禁止截面Gaussianize

训练铁律 (2026-07-15):
  训练窗口2-3年, 禁止2023年前数据
  每月第一个周一重训, IC≥0.03才替换

架构铁律 (v4.0):
  铁律#12: GBDT优先, 不做DL
  Alpha层: "LGBM + XGBoost + CatBoost Meta — 唯一可变层"
```

### 治理隐含了什么 (没写但必须成立的前提)

| 隐含前提 | 现状 | 成立? |
|------|------|:--:|
| 两模型用同一套因子 | LGBM=63, XGB=8 | ❌ |
| 两模型用同一种标签 | LGBM=CSRank, XGB=二分类 | ❌ |
| 两模型分数可比 | LGBM=0.2-100, XGB=1.9-71 | ❌ |
| 秩平均>单模型 | 没验证过 | ❓ |
| 两模型预测低相关 | 没测过 | ❓ |

**三个前提不成立 → 治理方案从未真正被执行过。**

---

## 二、治理方案的问题挖掘

### 问题1: "2模型" — 为什么是2个?

```
治理说: "干净2模型秩平均"
行业说: 华泰用50个LGBM (同模型×不同特征子集)
       广发用multi-loss合成 (同模型×不同loss)
       
关键差异:
  治理: 异模型 × 同因子 (GBDT结构差异→互补)
  行业: 同模型 × 异输入 (消除单一视角偏差)

谁对?
  治理的前提(同因子)从来没满足过 → 没跑过真实验
  行业的前提(50子模型)我们不适用 → 算力不够

诚实答案: 没验证过2模型秩平均是否优于单模型
```

### 问题2: "秩平均" — 均值比投票好吗?

```
投票 (当前, 坏了):
  LGBM>70 AND XGB>70 → 通过
  问题: 阈值敏感, 二值信息损失, 共识0%

秩平均 (治理建议):
  (LGBM% + XGB%) / 2 → combined
  问题: 两个模型看不同东西 → 均值无意义
       修完同因子后: 两模型看同一套东西
       → 预测高度相关 → 均值≈单模型
       → 多一个模型没有增量信息

秩平均只在两模型"看到不同但互补的信息"时有收益。
同因子同标签 → 它们的输出会高度相关 → 秩平均≈单模型。
```

### 问题3: Ridge — 角色错位

```
治理说: Alpha层=LGBM+XGB+CatBoost Meta, 没提Ridge
代码里: Ridge当作第三个选股模型, 权重33%

问题:
  Ridge是线性模型, 用8因子做线性回归→预测收益
  线性模型的IC不可能超过GBDT (非线性)
  → Ridge的预测≈LGBM/XGB的弱化版
  → 不是"低相关仲裁者", 是"更弱的第三个重复"

如果LGBM和XGB本来就有分歧:
  Ridge弱 → 倾向均值 → 无法打破平局 → 仲裁失效
如果LGBM和XGB高度一致:
  Ridge肯定也一致 → 不需要仲裁

Ridge的当前位置: 有用但不在正确的位置。
应该: 不参与选股排名, 只做校准(分数到仓位映射)。
```

### 问题4: "封顶" — 做到什么程度算封顶?

```
治理说: ML选股⭐⭐只做便宜的
现状:  我们今天加了PurgedKF+DSR+幸存者(已经超出"便宜"范围)

"封顶"的边界在哪?
  已做: ✅ GBDT双模型 ✅ CSRank ✅ PurgedCV ✅ DSR ✅ 幸存者
  应做: ✅ 因子池统一 ✅ 标签统一 ✅ 秩平均
  不应做: ❌ Meta LR ❌ 多智能体 ❌ 新模型 ❌ 滚动训练管线(太复杂)
  
封顶线: 因子池统一+标签统一+秩平均 = 便宜正确性完成
```

---

## 三、行业验证

### 行业对"2模型秩平均"的态度

```
游资:    单模型就够了, ML只是初筛 (✅ 治理对齐)
中小私募: 2-3模型+Meta融合 (⚠️ 治理建议的秩平均=简化版, 可以)
机构:    多模型Stacking (❌ 治理明确不做)

结论: 治理的"2模型秩平均"在游资↔私募之间, 定位准确。
     问题不是定位错了, 是从来没按这个定位执行过。
```

### 验证过的结论

| 做法 | 治理说 | 行业说 | 一致? |
|------|------|------|:--:|
| GBDT双模型 | ✅ | ✅ | ✅ |
| CSRank标签 | ✅ | ✅ | ✅ |
| 不做NN/DL | ✅ | ✅ (我们没GPU) | ✅ |
| 秩平均融合 | ✅ | ⚠️ (行业更爱Meta) | ⚠️ |
| ≤50因子 | ✅ | ✅ | ✅ |
| CatBoost禁用 | ✅ | ⚠️ (行业常用) | ⚠️ |
| 不做Meta LR | ✅ | ❌ (私募标配) | ❌ 待辩 |

### 行业用Meta LR但我们不用的理由

```
行业使用Meta LR的前提:
  ① 4+底层模型 → 权重学习有意义
  ② 每月重训 → Meta权重可更新
  ③ 独立验证集 → 防止Meta过拟合

我们的现实:
  ① 只有2个GBDT → 学2个权重≈手工调比例
  ② 每月重训已规划 → 但Meta也要重训, 维护成本翻倍
  ③ 2模型预测高度相关时 → Meta会学到50/50≈等权

结论: 2模型场景下, Meta LR≈等权≈秩平均。
      行业是对的, 但对我们不适用。
      等模型≥4个时再引入Meta LR。
```

---

## 四、最优组合

### 终态: 治理方案修正版

```
选股层 (ML初筛):
  统一8因子 → LGBM(CSRank, seed=42) → 原始分 → min-max → 百分位
  统一8因子 → XGBoost(CSRank, seed=42) → 原始分 → min-max → 百分位
  
  融合: (LGBM% + XGB%) / 2 → combined_score
       不是投票, 不是Meta, 就是秩平均
       
  Ridge: 退出选股排名
         → 改为校准器: 把combined_score映射到仓位百分比
         → 角色明确: 不是第三个模型, 是分数→仓位的翻译器

  门控: combined_score ≥ 65 → 进入候选池
        (单阈值, 不投模型票)
        共识门控(min_consensus=2)保留——但作用变了:
        不再用于"两个GBDT是否同意"
        而是"GBDT+Ridge(校准后)是否都在建议区间"
```

### vs 治理原文对照

| 维度 | 治理原文 | 修正 | 理由 |
|------|------|------|------|
| 因子池 | 隐含统一, 未明确 | **明确定8因子** | LGBM超标63→8 |
| 标签 | CSRank | **CSRank (不改)** | 治理正确 |
| 融合 | "秩平均" | **秩平均 (不改)** | 治理正确 |
| Ridge | 未定位 | **校准器,非选股** | 角色错位修正 |
| 门控 | 未明确 | **单阈值, 不投票** | 2模型投票太粗糙 |
| CatBoost | 禁用 | **禁用 (不改)** | 治理正确 |
| 因子数 | ≤50 | **8 (不改)** | 已达标 |

---

## 五、验证条件

### 秩平均必须通过以下测试才上线:

```
① 两模型预测相关 < 0.8 → 互补有效, 秩平均有增量
   若 ≥ 0.8 → 退化为单模型, LGBM优先(XGB冗余)

② 秩平均 IC ≥ max(单模型IC) → 融合有收益
   若 < → 直接用最好的单模型

③ 回测加成本(双边0.15%), Sharpe > 1.0 → 可上线
   若 < → 纯ML选股在当前市场无效, 靠策略confirm_xxx救
```

### 如果验证失败:

```
降级方案: 只用LGBM (IC=0.456)
          → XGBoost保留作备用 (模型退化时切换)
          → Ridge保留作校准
          → 不投票, 不融合, 单模型输出
          
理由: 一个可靠的模型 > 两个不可靠模型的和
```

---

## 六、终审

```
最优组合:
  LGBM + XGBoost (同8因子, 同CSRank) → 秩平均 → combined_score
  Ridge: 校准器 (分数→仓位)
  CatBoost: 禁用
  
  ✅ 对齐治理定盘星 (干净2模型秩平均, 便宜)
  ✅ 对齐行业 (GBDT双模型, CSRank)
  ✅ 修正了治理的遗漏 (因子池统一, Ridge角色)
  
改动量:
  LGBM改用8因子+CSRank重训 (改optuna_tune_lgbm.py)
  XGB改CSRank标签重训     (改optuna_tune_xgb.py)  
  Ridge退出选股改校准     (改generate_signal_table.py)
  门控从投票改单阈值      (改generate_signal_table.py)
  
  ≈ 4文件, ~100行
```
