# 潜龙风控总闸设计方案 v1.0

> 对标: 头部私募(多层防御) + RiskGuard(开源) + HFT Kill Switch 行业标准
> 日期: 2026-07-08 | 状态: 待审批

---

## 一、行业对标

### 1.1 行业标准 — 五层纵深防御

```
┌─────────────────────────────────────────────────┐
│ L1 策略级 │ 单策略止损/止盈/信号异常检测          │
│ L2 风险引擎 │ P&L上限/回撤熔断/仓位集中度         │ ← 独立进程, 不与策略同机
│ L3 执行网关 │ 下单速率/撤单率/异常单笔金额         │
│ L4 基础设施 │ 网络断开/服务器重启/硬件级断电       │
│ L5 交易所级 │ QMT/券商 前置风控 (SEC 15c3-5)      │
└─────────────────────────────────────────────────┘
```

来源: [Kill Switch Mechanisms in HFT Systems](https://algotradingdesk.com/kill-switch-mechanisms-hft-risk-control/), [RiskGuard](https://github.com/SilentFleetKK/riskguard)

### 1.2 核心原则（行业共识）

| 原则 | 含义 |
|------|------|
| **减仓永远放行** | 熔断中不阻止风险收敛（卖单照过，买单拦截） |
| **人工复位强制** | Kill Switch 触发后必须人工确认才能重启 |
| **多级不单点** | 软件 + 文件 + 配置 三重保护，单点失效不致命 |
| **审计不可篡改** | 每次熔断/复位记录哈希链日志 |
| **独立于策略** | 风控引擎不跑在交易策略同一进程 |

### 1.3 个人量化 vs 机构的关键差异

| 维度 | 机构 | 潜龙（个人） |
|------|------|------|
| 执行通道 | 多网关/多券商 | QMT + live_trader 双通道 |
| 监控方式 | 7×24 团队轮值 | 打开网页即见 |
| 物理按钮 | 交易台实体按钮 | **网页一键红色按钮** |
| 恢复速度 | 团队决策, 分钟级 | **单人判断, 一键开关** |
| 复杂度 | 5层全上 | 核心3层（策略+风险+执行）足够 |

---

## 二、当前系统现状

### 2.1 已有的风控能力

| 能力 | 位置 | 状态 |
|------|------|:--:|
| 三级止盈 | `trade_config_master.json` take_profit | ✅ |
| 两级止损 | `trade_config_master.json` stop_loss | ✅ |
| 日内风控 | `daily_risk` (-3%半仓, -5%清仓) | ✅ |
| 自动熔断 | `app.py:549-568` 5分钟策略熔断检查 | ✅ |
| 紧急清仓 | `/api/emergency/liquidate` | ✅ |
| QMT 日限额 | `auto_trade_plan.json` max_daily_trades=5 | ✅ |
| 动态仓位 | `market_adaptive` 牛市15%/震荡10%/熊市5% | ✅ |
| 模拟盘保护 | `paper_engine` T+1/涨跌停 | ✅ |

### 2.2 缺失的能力

| 缺失项 | 严重度 |
|------|:--:|
| ❌ 网页端一键总闸 | 🔴 |
| ❌ QMT 快速通道独立开关 | 🔴 |
| ❌ live_trader 自动交易独立开关 | 🔴 |
| ❌ 总闸状态可视化（QMT看不到自己开关状态） | 🟡 |
| ❌ 开关事件审计日志 | 🟡 |
| ❌ 人工复位强制（熔断后可自动恢复） | 🟡 |
| ❌ 手机告警（已有钉钉, 但熔断事件未接入） | 🟡 |

---

## 三、设计方案

### 3.1 总架构 — 三层开关

```
┌──────────────────────────────────────────────────────────┐
│                    🚨 总闸 (Master Kill Switch)            │
│             控制所有实盘交易通道的总开关                    │
│             关 = QMT + live_trader + 未来一切通道 全部停     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ⚡ QMT 快速通道开关          🤖 AI 自动交易开关            │
│  passorder() 执行开关         live_trader.py 策略循环      │
│  开 → QMT自动下单             开 → AI自动交易              │
│  关 → 只推信号不下单           关 → 只监控不交易             │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                    底层保障（不受开关控制）                  │
│  止损/止盈 永远执行 | 减仓单 永远放行 | 风控熔断 永远生效    │
└──────────────────────────────────────────────────────────┘
```

### 3.2 开关逻辑矩阵

| 总闸 | QMT快速 | AI自动 | QMT行为 | live_trader行为 |
|:--:|:--:|:--:|------|------|
| 🔴 关 | - | - | **只推信号, 不下单** | **只监控, 不交易** |
| 🟢 开 | 🔴 关 | 🟢 开 | 只推信号 | AI自动交易 |
| 🟢 开 | 🟢 开 | 🔴 关 | 自动下单 | 只监控 |
| 🟢 开 | 🟢 开 | 🟢 开 | 自动下单 | AI自动交易 |

### 3.3 数据文件设计 — 单一真相源

新增文件: `D:\quant_framework\kill_switch.json`

```json
{
  "version": "1.0",
  "master_kill": false,
  "qmt_fast_channel": true,
  "ai_auto_trade": false,
  "last_changed_by": "老板",
  "last_changed_at": "2026-07-08 14:30:00",
  "reason": "手工打开QMT快速通道",
  "audit_log": [
    {"time": "2026-07-08 09:26:00", "action": "master_kill", "from": true, "to": false, "who": "system", "why": "日初自动复位"},
    {"time": "2026-07-08 14:30:00", "action": "qmt_fast_channel", "from": false, "to": true, "who": "老板", "why": "行情恢复, 重新启用"}
  ],
  "auto_reset": {
    "enabled": true,
    "reset_at": "09:00",
    "description": "每日09:00自动复位总闸为关, 手动打开后才生效"
  }
}
```

### 3.4 各模块读取方式

| 模块 | 读取字段 | 频率 | 行为 |
|------|------|------|------|
| **QMT 策略** | `master_kill` + `qmt_fast_channel` | 每 `handlebar` 检查 | `master_kill=true` 或 `qmt_fast_channel=false` → 跳过 passorder |
| **live_trader.py** | `master_kill` + `ai_auto_trade` | 每轮循环检查 | `master_kill=true` 或 `ai_auto_trade=false` → 跳过买入 |
| **Flask 审批** | `master_kill` | 每次 `/api/signal/approve` 检查 | `master_kill=true` → 拒绝所有审批 |
| **首页** | 全部字段 | 每30秒轮询 | 显示三开关状态 + 熔断原因 |

### 3.5 安全设计 — 四重保护

```
第1重: kill_switch.json 文件     ← 所有模块读取, 最可靠
第2重: auto_trade_plan.json      ← QMT 兜底读取 circuit_breaker
第3重: trade_config_master.json  ← live_trader 兜底 auto_trade_enabled
第4重: QMT/券商 前置风控          ← 交易所级保护（不可绕过）
```

**总闸关闭**时:
- 所有新买单 → ❌ 拒绝
- 所有止损/止盈卖单 → ✅ 放行
- QMT → `handlebar` 照常跑, 信号照推, 但不调 `passorder`
- live_trader → 照常监控, 但不调买入
- 网页审批 → 返回 "总闸已关闭, 无法审批"

### 3.6 QMT 策略改动（最小侵入）

在 `_handlebar_impl()` 的快速通道段前加一行检查：

```python
# 读总闸状态 (文件小, 每次读不贵)
import json as _kj
_kill = {"master_kill": False, "qmt_fast_channel": True}
try:
    with open(r"D:\quant_framework\kill_switch.json", "r") as _kf:
        _kill = _kj.load(_kf)
except: pass

if _kill.get("master_kill", False) or not _kill.get("qmt_fast_channel", True):
    print(f"[快速] ⛔ 总闸关闭, 跳过下单")
    continue
```

### 3.7 Flask API 设计

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/kill-switch/status` | GET | 返回三个开关状态 + 最近变更记录 |
| `/api/kill-switch/toggle` | POST | 切换指定开关 `{"switch":"master_kill","value":true}` |
| `/api/kill-switch/reset` | POST | 日初自动复位 (09:00 cron) |

---

## 四、网页 UI 设计

### 4.1 位置

首页 `⚡ 盘中信号` 区域上方, 或独立一行。三开关并排显示。

### 4.2 视觉设计

```
┌─────────────────────────────────────────────────────────┐
│  🚨 总闸  [🟢 已开启]    ⚡ QMT快速  [🟢 自动]    🤖 AI自动  [🔴 关闭]  │
│                         震荡 ML>75 pos≤10%              │
└─────────────────────────────────────────────────────────┘
```

- 绿色 = 开, 红色 = 关, 黄色 = 部分限制
- 点击开关 → 二次确认弹窗 → 执行 → 状态即时更新
- 总闸关闭时, 整体区域红色闪烁背景

### 4.3 交互流程

```
点击 "总闸" → 确认弹窗 "确定关闭所有实盘交易?"
                ├─ 确认 → POST /api/kill-switch/toggle 
                │         → 写入 kill_switch.json
                │         → QMT 下次 handlebar 读到 → 停
                │         → live_trader 下次循环读到 → 停
                │         → 首页状态变红
                └─ 取消 → 不变
```

---

## 五、与蓝图对齐

| 蓝图条款 | 如何满足 |
|------|------|
| 铁律#14 参数三层治理 | kill_switch.json = 第2层(优化层), master配置永不自动改 |
| 人工在回路 | 总闸必须人工复位, 系统不自启 |
| 单一真相源 | kill_switch.json 是所有模块的唯一开关来源 |
| 不跳模拟上实盘 | 总闸默认关, 每日手工开 |

---

## 六、实施路线

### Phase 1: 最小可用 (2h)

- [ ] 创建 `kill_switch.json` + 读写逻辑
- [ ] Flask API: `/api/kill-switch/status` + `/api/kill-switch/toggle`
- [ ] QMT 策略: 快速通道前加总闸检查（5行代码）
- [ ] live_trader.py: 循环中加总闸检查（3行代码）
- [ ] 首页: 三开关 UI + 状态显示

### Phase 2: 完善 (1h)

- [ ] 审计日志写入 + 哈希链
- [ ] 钉钉告警接入（熔断事件推送手机）
- [ ] 日初自动复位
- [ ] 开关状态持久化（重启不丢）

### Phase 3: 增强 (按需)

- [ ] 分级熔断（预警→半开→全熔断）
- [ ] 自动熔断条件（日内亏损 N% 自动拉总闸）
- [ ] 手机端遥控开关

---

## 七、竞品对比

| 能力 | 潜龙(方案后) | vnpy | QMT原生 | BigQuant | 私募自研 |
|------|:--:|:--:|:--:|:--:|:--:|
| 总闸一键开关 | ✅ 网页 | ❌ | ❌ | ❌ | ✅ |
| 多通道独立控制 | ✅ QMT+AI | ❌ | ❌ | ❌ | ✅ |
| 状态可视化 | ✅ 首页绿灯 | ⚠️ | ❌ | ❌ | ✅ |
| 减仓永远放行 | ✅ | ❌ | ❌ | ❌ | ✅ |
| 审计日志防篡改 | ✅ 哈希链 | ❌ | ❌ | ❌ | ✅ |
| 手机告警 | ✅ 钉钉 | ❌ | ❌ | ❌ | ✅ |
| 人工复位强制 | ✅ | ❌ | ❌ | ❌ | ✅ |
| 个人可行(零依赖) | ✅ | ✅ | ✅ | ✅ | ❌ |

**独有优势**: 行业标准的多层防御在个人系统中首次完整实现。总闸+子通道+可视化+审计+告警, 对标私募无缺口。

---

## 关联

- [[architecture-upgrade-guide]] 12.4 — QMT执行架构
- [[trade_config_master.json]] — 参数配置
- [[task-master-20260707]] — 任务总书
- [[session-20260708-summary]] — 今日会话
