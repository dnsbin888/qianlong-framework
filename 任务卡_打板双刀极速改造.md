# 任务卡: 打板双刀极速改造 (一封+二封 <20ms)

**日期**: 2026-07-18  
**优先级**: P0 (策略A2核心交付)  
**状态**: 待周一验证→施工  
**依赖**: [QMT条件单API验证](任务卡_QMT条件单API验证.md) (已完成: API不存在,走subscribe_quote)

---

## 背景

当前打板双刀跑在 QMT 策略 handlebar 里, 1分钟K线触发, 延迟 0-60 秒。竞品游资用 L2 tick + 极速柜台做到毫秒级。

经过三天讨论+源码审查+行业调研:
- QMT 条件单 API: ❌ 不存在 (全行业都没有)
- 游资真实做法: L2 tick 推送 → Python 判断 → 即时下单
- 我们有现成基础设施: `qmt_quote_bridge` (行情) + `QMTBroker` (下单)

## 最终方案

```
双进程分离架构:

Flask 进程:                              QMT 策略进程:
  subscribe_whole_quote 全市场推送         handlebar (不变)
    → tick 回调                             → 盘中突破/竞价抢筹/尾盘急拉
    → 一封: 7道全保留 (~15ms)                → 板后接力/撬板战法/TDX信号/弱转强
    → 二封: 完整 confirm_board (~15ms)       → passorder()
    → xt_trader.order_stock()
```

| 维度 | 改造前 | 改造后 |
|------|--------|--------|
| 一封延迟 | 0-60s | ~15ms |
| 二封延迟 | 0-60s | ~15ms |
| 一封防护 | 7道(板块+封单+盘口) | 7道全保留 |
| 二封防护 | 6因子+动态阈值 | 完整保留 |
| API 条件单 | ❌ 不存在 | 不需要 |

## 施工步骤

### 前置验证 (周一,10分钟,P0阻塞)

- [ ] **1.1** 启动 QMT 勾选极简模式, 验证策略能否同时运行
- [ ] **1.2** Flask 进程里 `QMTBroker.connect()` 验证 xt_trader 连接
- [ ] **1.3** 假单全链路: tick回调 → order_stock → 模拟盘成交

> 如果 1.1 失败 (极简模式与策略冲突): 降级为 bar周期缩到5秒方案
> 如果 1.2 失败: 检查 QMT 版本/userdata_mini 路径/白名单
> 全部通过 → 正式施工

### Phase 1: 桥接打板核心 (1-2天)

- [ ] **P1-1** `qmt_quote_bridge.py` 增加打板检测模块
  - 预加载: 全市场涨停价表 + 流通盘表 + 板块映射 (init 时,~5秒)
  - L1 过滤: 回调内快速筛涨幅 (>9.5%), 标记 `_near_limit`
  - L2 加工: 独立线程 50ms 轮询 `_near_limit`, 跑 get_full_tick + confirm
  
- [ ] **P1-2** 一封逻辑迁移
  - 触板检测: lastPrice >= limit_up * 0.995
  - 7道防护: 时间窗 + 情绪退潮 + 涨跌比 + 板块联动 + 封单比 + 盘口失衡
  - 日限3笔 + 同票锁 + 情绪仓位联动
  - 保留现有信号ID格式 `db_{symbol}_{date}`

- [ ] **P1-3** 二封逻辑迁移
  - 炸板状态机: NORMAL → SEALED → BLOWN → RESEAL
  - 炸板检测阈值: price < limit_up * 0.98
  - 回封检测: price >= limit_up (tick级)
  - confirm_board(6因子+动态阈值) 前置判定
  - 日限2笔 + 板块热度 + 熊市保护 + 冷却60s

- [ ] **P1-4** `QMTBroker` 对接
  - `place_order()` 增加打板专用下单方法 (市价追入)
  - 成交回调 → 更新 `_daban_bought` + `_daban_daily`
  - 炸板自动撤单: 封单量暴跌→撤回未成交委托

### Phase 2: 数据桥接 (0.5天)

- [ ] **P2-1** 板块热度假数据生成
  - `generate_signal_table.py` 增加 `sector_bd_count` / `sector_hot` 字段
  - 写入 `qmt_trade_config.json`, 桥接进程读取

- [ ] **P2-2** 双进程互斥文件
  - `D:\quant_web\data\today_trades.json` 共享交易日记录
  - QMT 策略买入前检查 + 买入后写入
  - 桥接进程同理

### Phase 3: QMT 策略端清理 (0.5天)

- [ ] **P3-1** `signal_config.json` 增加打板开关
  ```json
  "qmt_builtin": {
    "打板·一封": false,
    "打板·二封": false
  }
  ```
  桥接正常时关, 桥接挂了手动开

- [ ] **P3-2** `qmt_full_strategy.py` 打板代码保留但不执行
  - 读 signal_config, enabled=false → 跳过打板块
  - 其他信号逻辑不动

- [ ] **P3-3** 前端 `generate_signal_table.py` 更新
  - signal_limits 保留打板标签 (前端仍显示)
  - 但来源标记 "bridge" vs "qmt"

### Phase 4: 压测+监控 (1天)

- [ ] **P4-1** 模拟盘全链路压测
  - 一封: 10只触板股, 验证7道过滤+下单延迟
  - 二封: 5只炸板回封, 验证状态机+confirm_board
  - 极端行情: 100只触板(千股涨停), 验证回调不堆积

- [ ] **P4-2** 监控告警
  - 桥接心跳: 30s 无 tick → 钉钉告警
  - 下单失败: order_stock 返回 -1 → 钉钉告警
  - QMT 策略兜底: 桥接离线超 60s → 自动开打板开关

- [ ] **P4-3** 快照备份
  - 施工前: `snap_20260719_pre_daban`

## 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `qmt_quote_bridge.py` | 🔧 改造 | 加打板检测模块 |
| `qmt_broker.py` | 🔧 改造 | 加打板专用下单+撤单 |
| `generate_signal_table.py` | 🔧 改造 | 加板块热度+打板来源标记 |
| `signal_config.json` | 🔧 改造 | 加打板独立开关 |
| `qmt_full_strategy.py` | 🔧 改造 | 打板代码加开关,不删 |
| `trade_config_master.json` | 📖 读取 | 打板参数阈值 |
| `qmt_trade_config.json` | 📖 读取 | 候选股+情绪+板块数据 |
| `today_trades.json` | 🆕 新建 | 双进程互斥文件 |
| **所有文件** | 📸 快照 | 施工前 snap |

## 验收标准

- [ ] 一封延迟: tick→下单 <20ms (模拟盘实测)
- [ ] 二封延迟: 回封触板→下单 <20ms (模拟盘实测)
- [ ] 一封 7 道防护: 板块联动/封单比/盘口失衡 全部生效
- [ ] 二封 6 因子: 时间×封单×炸板×回封×板块×动态阈值 全部生效
- [ ] 桥接离线 → QMT 策略自动兜底 (手动开开关)
- [ ] 模拟盘连跑 3 天无崩溃
- [ ] 与旧版打板追封对比, 延迟改善 ≥100 倍 (实测数据)

## 关联

- [[策略ABC路线图]] — A2打板双刀, 与A1弱转强并列
- [[QMT优化决策]] — Flask做精, 不碰QMT架构
- [[待完成任务卡]] — 弱转强交付后残余+基建缺口
- [[任务卡_QMT条件单API验证]] — 前置调研, 已完成
- [[行业对标报告 v1.0]] — 七维51/60分, 打板极速是加分项
