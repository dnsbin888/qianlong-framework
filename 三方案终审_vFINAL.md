# 三方案终审: 打分融合 vs 百分位排序 vs 加减分

**日期**: 2026-07-19
**背景**: 多策略×多通道 架构下, ML选股的最优做法
**方法**: 治理 × 行业 × 实测 × 架构适配度

---

## 一、三方案定义

### 方案A: 打分融合

```
LGBM得分 + XGB得分 + Ridge得分 → 加权平均 → combined → ≥65 → 通过
↓
一个分数统治所有策略
```

### 方案B: 百分位排序

```
LGBM CSRank → 0-1百分位 → Top150 ┐
XGBoost CSRank → 0-1百分位 → Top150 ├→ 并集 → 候选池
Ridge CSRank → 0-1百分位 → Top150 ┘
↓
策略从池里挑
```

### 方案C: 加减分

```
LGBM原始分 → ≥60通过 ┐
XGB归一化分→ ≥60通过  ├→ 并集 → 候选池
Ridge归一化→ ≥60通过 ┘
↓
策略从池里挑, 策略可自设门槛
```

---

## 二、七维测评

### 维度1: 多策略适配

```
场景: 7个策略各有自己的confirm逻辑

方案A: combined≥65 → 统一门控
       → 所有策略共享同一个候选人名单
       → 打板(care封板)和弱转强(care竞价)用同一份名单
       → 不灵活 ⚠️

方案B: 统一Top150
       → 同上, 所有策略共享同一份Top-N
       → N固定, 不灵活 ⚠️

方案C: 每策略设自己的门槛
       → 打板门槛低(LGBM≥40, 快速扫)
       → 二封门槛高(LGBM≥80+XGB≥60, 精准)
       → 弱转强依赖confirm_wts(ML只是初筛)
       → 最灵活 ✅
```

| | A | B | C |
|:--|:--:|:--:|:--:|
| 策略自设门槛 | ❌ | ❌ | ✅ |
| 不同策略不同严格度 | ❌ | ⚠️ | ✅ |
| 策略可跳过ML | ❌ | ❌ | ✅ |

### 维度2: 多通道适配

```
方案A/B: ML分数 → combined → 单管道输出
        → QMT/信号表/TDX三条通道都吃同一份ML输出
        → 通道间没有差异化

方案C: QMT通道(打板): 低门槛快速候选
       信号表通道(反转): 中门槛+confirm裁决
       TDX通道: 跳过ML, 独立运行
       → 每个通道有自己的候选标准和裁决逻辑 ✅
```

### 维度3: 今日实测

```
实测数据: LGBM 90-100(窄), XGB 61-70(窄)

方案A: avg(LGBM=100, XGB=70)=85 < LGBM单=96
       → 平均惩罚多模型共识 🔴
       → Top20: 只有50%重叠

方案B: LGBM百分位 + XGB百分位
       → 两者都是同尺度(0-1)
       → XGB 61-70变百分位后也是0.5-0.8
       → OK, 但丢失幅度信息

方案C: LGBM≥60→全过(90-100都在60以上)
       → XGB归一化后还是挤在60-70
       → 设门槛=65→XGB只过一半
       → 策略可独立调整门槛 ✅
       → 不丢失幅度信息 ✅
```

### 维度4: 治理对齐

```
定盘星: "ML选股⭐⭐只做便宜的·封顶"
ABC路线图: "ML排名能排序不能判方向。保留作候选池筛选。"

方案A: ML做最终决定→违背治理 🔴
方案B: ML做排序→对齐治理 ✅
方案C: ML做初筛→完全对齐治理 ✅✅ (最便宜)
```

### 维度5: 行业对齐

```
游资: ML→初筛→人工终判 (ML不是决策者)
私募: ML→Meta融合→组合优化
前沿: 端到端RL

方案C对齐游资 ✅ (我们定位=个人量化×游资)
```

### 维度6: 风险与复杂度

```
方案A: 
  风险: 尺度不统一的平均 → 失真
  复杂度: 需维护三模型融合+门控参数
  维护: XGB改版→整个combined管道要调

方案B:
  风险: 百分位丢失幅度信息
  复杂度: 截面排序→每批数据都要排
  维护: XGB改CSRank→百分位跟着变

方案C:
  风险: 门槛设定依赖经验
  复杂度: 最低—每个模型独立判断
  维护: XGB改版→只改XGB自己的门槛
```

### 维度7: 可进化性

```
方案A: 模型增加→融合维度增加→越来越重
方案B: 模型增加→多个Top-N并集→简单
方案C: 模型增加→多加一条及格线→最简单

未来方向:
  CatBoost复活(Meta裁判)→方案C直接加一行
  新策略加入→方案C只需加该策略的ML门槛配置
```

---

## 三、综合评分

| 维度 | 权重 | 方案A | 方案B | 方案C |
|------|:--:|:--:|:--:|:--:|
| 多策略适配 | 20% | 2 | 2 | **5** |
| 多通道适配 | 15% | 2 | 2 | **5** |
| 实测表现 | 20% | 1 | 3 | **4** |
| 治理对齐 | 15% | 1 | 4 | **5** |
| 行业对齐 | 10% | 2 | 3 | **4** |
| 复杂度 | 10% | 2 | 3 | **5** |
| 可进化 | 10% | 2 | 4 | **5** |
| **加权总分** | 100% | **1.7** | **2.8** | **4.7** |

---

## 四、终审

```
方案C (加减分): 最优

理由:
  ① 治理给它最高分 ("ML只做便宜辅助")
  ② 多策略天然适配 (每个策略设自己的门槛)
  ③ 多通道天然适配 (各通道独立)
  ④ 风险最低 (改动最小, 退路最多)
  ⑤ 行业对齐 (游资做法: ML初筛+人工/策略终判)
```

## 五、方案C 终态

```
候选池生成:
  LGBM原始分≥60  → 标记
  XGB归一化≥60   → 标记
  Ridge归一化≥60 → 标记
  并集 → 全量候选池

各策略用法:
  打板·一封:  候选池中任1模型≥40 → confirm_board()
  打板·二封:  候选池中LGBM≥80+XGB≥60 → confirm_board()
  弱转强:     候选池中任1模型≥50 → confirm_wts()
  盘中突破:   候选池中LGBM≥50 → confirm_oversold()
  板后接力:   候选池中LGBM≥60 → 策略自身逻辑
  撬板战法:   候选池中任1模型≥40 → 策略自身逻辑

  策略可降门槛 (少候选→放宽) 可升门槛 (多候选→收紧)
  策略可跳过ML (TDX/竞价直通)
  
自动交易:
  非"ML分数≥XX"决定
  而是"策略confirm通过 + 满足风控"决定
```

## 六、实施

```
今天: LGBM保持现状(已是63因子CSRank), 
      generate_signal_table.py: 删average融合, 
      改为各模型独立标记过线, 策略从并集挑选
      
XGB: 后续改CSRank回归→分数拉开→独立设门槛
Ridge: 保持校准器角色
```
