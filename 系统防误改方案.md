# 系统防误改方案

**日期**: 2026-07-19
**目标**: 减小系统出错, 防止误改写

---

## 一、实盘安全锁

```
问题: trading_channel 改 "qmt" → 真金白银下单
防: 双重锁

① trading_channel="qmt"    ← 通道开关
② real_confirmed=true      ← 新增: 老板确认标志
③ master_switch.can_buy("real") ← 已有: 风控总闸

三重全开才执行实盘 → 改错一个不会触发
```

### 实施

```python
# live_trader.py CONFIG
"trading_channel": "ths",       # 通道 (qmt/ths)
"real_confirmed": false,        # 🆕 老板手动确认
"real_confirmed_at": "",        # 🆕 确认时间

# 下单前检查
if trading_channel == "qmt" and not real_confirmed:
    abort("实盘未确认, 拒绝下单")
```

## 二、关键文件只读锁

```
文件                         风险              保护
trade_config_master.json     被改参数          已锁定(宪法锁)
live_trader_config.json      改trading_channel 加确认字段
paper_account.json           数据丢失          已根治(六件套)
signal_config.json            策略开关被改      加校验
auto_trade_plan.json          被覆盖            原子写入已有
```

### 实施

```
启动时校验 live_trader_config.json:
  - trading_channel 非 "qmt" 或 real_confirmed=true → OK
  - trading_channel = "qmt" 且 real_confirmed=false → 告警+打印红字
  - paper_account.json 数据完整性 → 自动校验
```

## 三、写入前必快照

```
改前: 已有大部分
  paper_engine._save() → .bak ✅
  
改后: 扩展到
  generate_signal_table.py → signal_table.json →
    save前: signal_table.json → signal_table.json.bak
  app.py → 所有写配置操作 →
    save前: xxx.json → xxx.json.bak
```

## 四、Flask 启动完整性自检

```
✅ 已有: 9/9 核心文件锁定
🆕 新增:
  ① paper_account.json 数据校验 (cash+positions+trades一致性)
  ② QMT trading_channel 安全检查
  ③ 模型文件存在性校验  
  ④ signal_table.json / auto_trade_plan.json 存在性校验
```

