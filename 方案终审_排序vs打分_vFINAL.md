# 方案终审: 排序 vs 打分

**日期**: 2026-07-19
**方法**: 治理文件 × 行业主流 × 历史教训 × 今日实测 × 多策略架构

---

## 一、两个方案

| | 方案A: 打分+融合 | 方案B: 排序+分用 |
|------|------|------|
| **逻辑** | 多模型打分→融合→一个综合分 | 模型排序→候选池→策略自己挑 |
| **融合** | 平均/加权/投票 | 不融合 |
| **门控** | 综合分≥阈值→通过 | 无门控 |
| **策略关系** | ML分数决定一切 | ML只是候选服务 |

---

## 二、治理文件怎么说

### 定盘星 (2026-07-12)

```
优先级:
  游资执行: ⭐⭐⭐⭐⭐ 重投前沿 (核心alpha)
  ML选股:   ⭐⭐ 只做便宜的·封顶 (辅助排序)
  前沿ML:  ⭐ 暂缓

"干净2模型秩平均 + 诚实评测, 不追前沿ML"
```

**判断**: ML只做辅助排序。方案B(排序+分用) = ML是服务。方案A(打分+融合) = ML是决定者。**治理倾向于B。**

### 策略ABC路线图 (2026-07-16)

```
"ML排名能排序不能判方向。保留: 可作候选池筛选(Top30%缩小范围)。
不独立使用: 必须叠加方向过滤或策略择时。"
```

**判断**: 07-16就验证了ML不能独立做多。方案A让ML做最终决定。方案B让策略做最终决定。**路线图明确支持B。**

### ML信号铁律 (2026-07-15)

```
✅ CSRank做训练标签
✅ 原始分选股, per-model min-max归一化
❌ 禁止行业+市值中性化(损害树模型)
```

**判断**: 方案A的"平均"违背了per-model独立原则。**铁律倾向于各自排序。**

---

## 三、行业怎么说

| 层 | 做法 | 方案A (打分融合) | 方案B (排序分用) |
|------|------|:--:|:--:|
| **游资** | 单模型排序+人工终判 | ❌ 过度 | ✅ 一致 |
| **中小私募** | 2-3模型+Meta融合 | ✅ 相似 | ⚠️ 更简单 |
| **前沿** | Stacking+RL | ❌ 不够 | ❌ 不够 |
| **华泰金工** | 50个LGBM→分歧度因子 | ❌ | ⚠️ |

**判断**: 方案B对齐游资(我们的定位)。方案A对齐中小私募(但不是我们的定位)。

---

## 四、今日实测怎么说

### 数据事实

```
① 共识率: 0% (LGBM 50只 ∩ XGB 30只 = 0)
② 分数尺度不统一: LGBM=90-100, XGB=61-70
③ 平均惩罚多模型: LGBM=100+XGB=70→combined=79 < LGBM单=96
④ Top20重叠: 仅50%
⑤ XGB贡献: Combined独有的10只里, XGB全是空分数→完全是LGBM在排
```

### 方案A在实测中的表现

```
if 方案A (当前状态):
  combined = avg(LGBM, XGB, Ridge)  → 尺度不统一→惩罚多模型
  combined ≥ 65 → 通过
  自动交易 = 19只
  问题: 多模型好看的股票反被踢出前20

if 方案B:
  LGBM CSRank → Top300 → 候选池
  打板confirm_board → 从池里挑 → 买入/放弃
  弱转强confirm_wts → 从池里挑 → 买入/放弃
  自动交易 = 策略判出来的结果
  问题: 打板1只(其余策略0) — 是weekend数据少
```

---

## 五、多策略架构怎么说

### 方案A下的多策略

```
LGBM+XGB→平均→combined→门控→auto_trade_plan
                                    ↓
                              所有策略共用这个plan
                              策略没有自己的候选池
                              ML分数决定一切
```

### 方案B下的多策略

```
LGBM CSRank → 候选池(5000→300)
              ↓
    打板策略: confirm_board → 打板通道
    反转策略: confirm_wts + confirm_oversold → 信号表通道
    QMT实时: 打板·一封/盘中突破 → 不经过ML
    TDX公式: 独立引擎 → 独立通道
    
    每个策略有自己的入口, 互不阻塞
    ML瘫痪→打板/反转/竞价/突破照样跑
```

**判断**: 方案B天然适合多策略多通道。方案A的单管道设计会成为瓶颈。

---

## 六、历史教训怎么说

### CatBoost 教训 (2026-07-12)

```
"改几行复活"是错的。单快照训练+未来数据→训练退化。
安全路线 = 先禁用去噪 + 另立项带完整回测重建。
```

### CSRank 72%胜率教训 (2026-07-15→07-16)

```
07-15: CSRank 72%胜率/+9Sharpe → 庆祝
07-16: 审计发现前视偏差 → 真实=26.8%胜率/-20.77Sharpe
教训: "排名能排序不能判方向。不独立使用。"
```

### 方案A的复刻风险

```
方案A (打分融合): 让ML分数决定买入→和07-15的CSRank独立做多路线同构
方案B (排序分用): 让策略决定买入→和07-16总结的"候选池筛选"路线一致
```

**判断**: 方案A在走07-15的老路。方案B在延续07-16的结论。

---

## 七、融合哲学

### 方案A的融合理念

```
"模型越多越准" → 3模型都要同意 → 共识=质量
实测: 0%共识 → 模型越多≠越准
     LGBM=100 + XGB=70 → 平均=85 → 比LGBM单排的100弱
```

### 方案B的融合理念

```
不是融合模型。是融合判断。

ML做一层判断 (排序)
策略做二层判断 (终判)
两层独立, 各司其职。
不需要它们意见一致。
```

---

## 八、终审

| 维度 | 方案A (打分) | 方案B (排序) | 证据 |
|------|:--:|:--:|------|
| 治理定盘星 | ❌ | ✅ | ML只做便宜辅助 |
| ABC路线图 | ❌ | ✅ | ML不能判方向 |
| 历史教训 | ❌ | ✅ | CSRank独立做多失败 |
| 今日实测 | ❌ | ✅ | 平均惩罚多模型 |
| 多策略架构 | ❌ | ✅ | 策略需要独立入口 |
| 游资对标 | ❌ | ✅ | 我们不是私募 |
| 复杂度 | 高 | 低 | — |

**结论: 方案B (排序+分用)。**

```
终态:

LGBM(CSRank) → 百分位排序 → Top300 → 候选池
                                      ↓
                          策略A 从池里挑, confirm → 打板通道
                          策略B 从池里挑, confirm → 反转通道
                          策略C 直接做, 不经过ML  → QMT通道
                          策略D 独立公式          → TDX通道

XGBoost: 保留作备用排序源 (不需融合, 各排各的)
Ridge: 保留作校准器 (分数→仓位映射)
```

**改1处: generate_signal_table.py → LGBM直接排序输出候选池, 不做平均融合。**
