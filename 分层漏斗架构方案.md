# 潜龙分层漏斗架构方案 v1.0

> 对标: 量化私募主流 分层漏斗 + QuantConnect Alpha流 + 2025前沿
> 基准: 蓝图v3.0 · 铁律#12(GBDT优先) · 六层架构

---

## 一、行业标准：三层漏斗

```
5000只全市场
    ↓ 第1层: 量化筛选 (ML打分, 日线)
200只候选池
    ↓ 第2层: 深度分析 (技术+另类+基本面, 日线+分钟)
20只精选
    ↓ 第3层: 组合优化 (风险预算+行业中性+仓位分配)
5-8只持仓
```

| 层 | 私募主流做法 | 潜龙现状 | 差距 |
|:--:|------|------|:--:|
| **第1层** | 多因子ML 5000→200 | ✅ LGBM+XGB+CB 8872→89 | 达标 |
| **第2层** | 技术形态/资金流/龙虎榜/基本面 | ⚠️ 仅4个QMT硬编码条件 | **缺TDX/北向/龙虎榜** |
| **第3层** | 风险预算+行业中性+VaR上限 | ✅ 方案A 百分位×市场+板块≤30% | 达标 |

---

## 二、方案：三层漏斗完整化

### 第1层：ML 候选池（现有，不动）

```
全市场 8872只
  → LGBM(16因子) + XGBoost(8因子) → CatBoost Meta裁判
  → 百分位排序 → Top 89 候选
  → 输出: signal_table.json + auto_trade_plan.json
```

**门坎规则**：不在候选池的股票，第2层信号无效。

### 第2层：深度 Alpha 信号（扩建）

```
候选池 89只内:
  ├─ QMT 盘中信号 (已有): 竞价抢筹/盘中突破/尾盘急拉/打板追封
  ├─ TDX 公式池 (新增): 用户自定义通达信公式 → Python → QMT信号条件
  ├─ 北向资金因子 (待接入): akshare stock_hsgt_hold_stock_em → 持仓变化信号
  └─ 龙虎榜因子 (待接入): akshare stock_lhb_detail_em → 游资/机构动向信号

执行规则: 
  仅当股票 ∈ 第1层候选池 → 信号才进入快速通道
  不在候选池 → 仅审核通道记录, 不自动下单
```

### 第3层：组合优化（现有，不动）

```
方案A: 百分位仓位 × 市场系数 + 板块≤30%
三模型共识 → 全仓; 双模型 → 半仓; 单模型 → 审批
```

---

## 三、执行流程

```
每日09:00  第1层: generate_signal_table.py → 刷新候选池+auto_trade_plan
盘中实时  第2层: QMT检测信号 → 查候选池 → 在池内? 
           → 是: 快速通道 passorder (共识分级仓位)
           → 否: 审核通道 POST Flask (仅记录)
每日15:30  归档: 当日信号+成交 → 更新ML训练数据
```

---

## 四、TDX 公式接入规范

### 转换规则

通达信公式 → QMT Python 条件:

| 步骤 | 做法 |
|------|------|
| 1 | 用户提供通达信公式文本 |
| 2 | AI 转成 Python 函数 `def check_xxx(bar, prev_bar, history):` |
| 3 | 注册到 `TDX_SIGNALS` 字典 |
| 4 | QMT handlebar 中遍历检查 |
| 5 | 触发后查候选池 → passorder 或 POST |

### 信号增加流程

```
1. 用户贴公式 → AI转Python
2. 人工审核逻辑正确性  
3. 加入潜龙快速通道.py
4. QMT重启策略 → 生效
```

---

## 五、与现有系统整合

| 组件 | 改前 | 改后 |
|------|------|------|
| generate_signal_table.py | ✅ | 不变 (持续输出候选池) |
| auto_trade_plan.json | 仅快速通道用 | **TDX信号门坎** (查此文件判断是否在池) |
| 潜龙快速通道.py | 4个硬编码信号 | **+ TDX公式池** + 候选池过滤 |
| 审核通道 | POST记录 | 同, 标记"不在候选池" |
| 前端信号卡片 | QMT+ML分开显示 | 同, TDX信号标注来源 |

---

## 六、前沿适配 (2025-2026)

### 信号标准化接口

所有信号源统一格式，未来接入 LLM Agent/另类数据/任何新源零改动：

```python
# 标准信号格式 (所有源输出统一)
{
    "symbol": "sh600519",
    "source": "tdx_formula" | "qmt_trigger" | "northbound" | "lhb" | "llm_agent",
    "signal_type": "突破买入",        # 人类可读
    "confidence": 0.0-1.0,           # 信号源自身置信度
    "price": 1600.0,
    "timestamp": "2026-07-07 14:30",
    "meta": {}                        # 扩展字段
}
```

### 可扩展信号注册表

```python
# qmt_strategies/signal_registry.py
SIGNAL_SOURCES = {
    "qmt_builtin": QMTBuiltinSignal,    # 4个原有条件
    "tdx_formula": TDXFormulaSignal,     # 用户自定义公式
    "northbound": NorthboundSignal,      # 北向资金 (待接入)
    "lhb": LHBSignal,                    # 龙虎榜 (待接入)
    # 未来扩展:
    # "llm_agent": LLMAgentSignal,       # LLM Agent 因子
    # "sentiment": SentimentSignal,      # 舆情情绪
    # "macro": MacroSignal,              # 宏观因子
}
```

### 在线学习管线

```
每天收盘:
  当日成交数据 → 追加到训练集 → 周末自动重训 LGBM/XGB/CB
  Meta-Labeler 积累 100 笔后 → 每周更新信号可信度模型
```

### 不做的 (不追的)

- ❌ 多 Agent 协商 (个人系统无意义)
- ❌ 实时基本面数据 (获取成本高)
- ❌ 强化学习动态调参 (样本不足)

---

## 七、不做的

- ❌ TDX 公式替代 ML 选股 (ML 全局优化 > 单个公式)
- ❌ 另类数据独立出信号 (纳入因子注册表, ML 统一学习权重)
- ❌ 多 Agent 协商 (P2 探索, 个人系统无意义)
- ❌ 实时基本面数据 (个人获取成本高)

---

## 七、优先级

| 优先级 | 任务 | 投入 | 影响 |
|:--:|------|:--:|------|
| **P0** | TDX 公式转 QMT + 候选池过滤 | 1h/公式 | 直接增加盘中信号 |
| **P1** | 北向资金因子接入 | 2h | 新 Alpha 源 |
| **P1** | 龙虎榜因子接入 | 1h | 新 Alpha 源 |
| **P2** | 基本面因子 (PE/PB/ROE) | 4h | Alpha 多样性 |

---

## 八、总结

```
改前:  ML选股 → 4个固定信号 → 下单
改后:  ML选股 → TDX自定义+资金流+龙虎榜 → 候选池过滤 → 下单

核心变化: 信号源从4个 → 无限(公式可扩展), 但有ML候选池把关
```
