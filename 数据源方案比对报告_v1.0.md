# 数据源方案比对报告 v1.0

> 日期: 2026-07-09 | 对标: 私募主流 + 个人量化最佳实践 + 2025前沿

---

## 一、行业做法

### 1.1 私募(1-10亿)数据架构

```
L1 实时行情: 券商直连(XTP/QMT) — 微秒级
L2 历史日线: 数据厂商(Wind/Choice) — T+0
L3 基本面: 财报/研报 — T+1
L4 另类数据: 舆情/资金流向/龙虎榜 — 实时
```

**铁律**: 训推同源。训练模型和实盘预测必须用同一个数据源。不管数据好坏，只要一致，模型就学到正确的映射关系。换数据源 = 模型作废。

### 1.2 个人量化主流(2025)

| 数据源 | 占比 | 评价 |
|------|:--:|------|
| QMT xtdata | **70%** | 免费、合规、训推一体 |
| AkShare | 15% | 补财务/另类数据 |
| Tushare | 10% | 老牌，积分制 |
| TDX(mootdx) | 5% | 看盘工具，不宜做数据管道 |

来源: [知乎:个人投资者量化交易数据源推荐](https://www.zhihu.com/question/1955940776333808350), [52pojie:QMT数据方案](https://www.52pojie.cn/forum.php?mod=viewthread&tid=2075854)

### 1.3 前沿方向(2026)

- **多数据源融合** — QMT主 + akShare辅，分层使用
- **实时数据质量监控** — 自动检测数据漂移，切换备份源
- **云端数据同步** — 盘后自动上云，多机共享

---

## 二、潜龙现状

| 组件 | 当前数据源 | 问题 |
|------|------|------|
| 日线(parquet) | QMT (07-09刚切) | ✅ 新 |
| ML模型(LGBM/XGB/CB) | **TDX 旧数据训练** | 🔴 训推不同源 |
| 实时行情 | QMT tick | ✅ |
| 选股器(/screener) | QMT → TDX降级 | ✅ |
| 资金流向 | westock Node.js CLI | ✅ |
| 龙虎榜 | akShare | ✅ |
| TDX公式信号 | TDX tpool文件 | ✅ 保留 |

**核心矛盾**: 数据换成 QMT 了，模型还是 TDX 数据训的。sh600030 从 100 分掉到 23 分就是这个原因。

---

## 三、三方案比对

### 方案 A：全切 QMT（推荐）

```
QMT xtdata = 唯一日线数据源
  ├─ update_data_qmt.py → stock_data.parquet
  ├─ 重训 LGBM + CatBoost (基于QMT数据)
  ├─ generate_signal_table.py (训推同源)
  └─ QMT策略实时信号 (同一数据源)
```

### 方案 B：QMT + AkShare 双源

```
QMT xtdata → 日线+实时行情(主)
AkShare → 龙虎榜+北向资金+财务(辅)
TDX → 仅公式信号
```

### 方案 C：保持现状

```
不推荐 — 训推不同源，信号质量持续恶化
```

| 维度 | 方案 A | 方案 B | 方案 C |
|------|:--:|:--:|:--:|
| 训推同源 | ✅ | ✅ | ❌ |
| 实施代价 | 重训模型(10min) | +2h | 0 |
| 数据一致性 | ✅ | ✅ | ❌ |
| 长期维护 | 低 | 中 | 高 |
| 行业对标 | ✅ QMT主流 | ✅ 双源备份 | ❌ |
| ML评分稳定 | ✅ | ✅ | ❌ |
| **推荐** | ⭐⭐⭐ | ⭐⭐ | ❌ |

---

## 四、方案 A 实施步骤

### Step 1: 确认 QMT 数据完整 (已完成)

```
✅ update_data_qmt.py → 5202只 × 60天
✅ stock_data.parquet 最新 07-09
✅ generate_signal_table.py 63条信号
```

### Step 2: 重训 ML 模型 (待做)

```
python D:\quant_web\generate_signal_table.py  # 已完成，模型加载TDX旧模型
# LGBM训练在 lgbm_strategy.py 内
# CatBoost在 train_catboost.py
# XGBoost在 xgb_factor_weight.py
```

找到训练入口，基于 QMT parquet 重训。

### Step 3: 验证一致性

重训后跑 `generate_signal_table.py`，sh600030 评分应恢复合理水平。

---

## 五、收益预估

| 指标 | 当前 | 方案A后 |
|------|:--:|:--:|
| 训推一致性 | ❌ 不同源 | ✅ 同源 |
| ML评分稳定性 | ⚠️ 漂移 | ✅ 稳定 |
| 信号可信度 | ⚠️ 低 | ✅ 恢复 |
| 因子重要性 | 基于TDX | 基于QMT |
| 每日数据更新 | 手动 | 自动 |

---

## 六、决策建议

**选方案 A**。全切 QMT，重训三个模型。10分钟决策，长期零维护。
