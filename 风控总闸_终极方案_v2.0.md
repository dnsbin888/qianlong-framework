# 潜龙风控总闸 — 终极方案 v3.0 ✅ 100% 已实施

> 综合: 赵方案(三层开关) + 郑方案(FIA五层+三级预警) + 行业调研(私募/HFT/RiskGuard)
> 原则: 取三方最优，不留缺口
> 状态: **2026-07-08 全部实施完成**

---

## 一、三方案比对

### 1.1 架构理念

| 维度 | 赵方案 v1.0 | 郑方案 v1.0 | 行业标准 | **终极 v2.0** |
|------|:--:|:--:|:--:|:--:|
| 对标基准 | 私募多层防御 + RiskGuard | FIA 2024 五层 + 小型私募 | SEC 15c3-5 / MiFID II | **FIA 2024 + 私募实践** |
| 总开关形式 | 三层开关(总闸→子通道) | 独立模块 can_buy/can_sell | 物理+软件双备 | **can_buy/can_sell + 三层开关(取其精华)** |
| 分级预警 | ❌ 无 (二进制开/关) | ✅ 黄🟡橙🟠红🔴 三级 | ✅ 多级 | **✅ 黄橙红三级** |
| 减仓放行 | ⚠️ 提到但未实现 | ✅ can_sell() = True | ✅ FIA铁律 | **✅ can_sell() 永远 True** |
| 整合深度 | 每模块具体行号 | 模块接口层 | 进程级隔离 | **接口 + 行号 (两者互补)** |
| 状态持久化 | kill_switch.json | master_switch.json | 数据库/文件 | **master_switch.json (更干净)** |
| 审计日志 | 内嵌 kill_switch.json | 独立 audit.jsonl 哈希链 | 微秒级时间戳 | **audit.jsonl + 哈希链** |
| 页面设计 | 首页一行开关 | 独立 /risk-panel 全功能 | 独立监控台 | **独立 /risk-panel (功能全)** |
| 确认码 | ❌ 无 | ✅ 实盘开启需确认 | ✅ 双因素 | **✅ 确认码** |
| 紧急停止 | ❌ 无独立按钮 | ✅ 5秒倒计时可撤回 | ✅ 物理按钮 | **✅ 网页红色按钮+倒计时** |
| 风控预算 | ❌ 无 | ✅ 日亏损/连续亏损/行业集中度 | ✅ | **✅** |

### 1.2 评分为何如此

**赵方案优势**: 落地最具体。每处改几行都写清楚了，不会出现"方案通过但不知道改哪"的情况。开关逻辑矩阵简洁。
**赵方案弱点**: 太粗糙。二进制开/关，没有分级；减仓放行只提了概念没落地；没有风控预算引擎。

**郑方案优势**: FIA 2024 对标最完整。三级预警完整、can_buy/can_sell 接口干净、独立模块零依赖、确认码防误开。设计最接近私募实盘。
**郑方案弱点**: 具体落地细节不够 — 哪个文件改哪行、QMT 策略怎么接 master_switch、三色灯触发阈值如何动态调整，没有展开。

**行业标准**: 防御最深，但个人系统用不上硬件级 kill switch 和跨交易所风险引擎。

---

## 二、终极方案 — 全貌

### 2.1 核心模块

```
master_switch.py       ← 总闸控制器 (独立模块, 零依赖)
  ├─ can_buy(channel)  ← 所有买入前检查
  ├─ can_sell()        ← 永远 True (减仓不拦截)
  ├─ emergency_halt()  ← 一键全断
  └─ 自动读取 trade_config_master.json 阈值

risk_budget.py         ← 风控预算引擎
  ├─ 日亏损追踪
  ├─ 连续亏损追踪
  ├─ 行业集中度检查
  └─ 三色灯判定 (🟢🟡🟠🔴)

audit.jsonl            ← 审计日志 (哈希链, 防篡改)

/risk-panel            ← 独立 Web 操作台
  ├─ 三开关状态 + 风险仪表盘
  ├─ 分级预警实时显示
  ├─ 🚨 紧急停止按钮
  └─ 操作日志流
```

### 2.2 总架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                  潜龙风控操作台  /risk-panel                      │
│                                                                 │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐     │
│  │ 总闸     │ │ 实盘开关  │ │ 模拟开关  │ │ 🚨 紧急停止      │     │
│  │ 🟢/🔴    │ │ 🟢/🔴     │ │ 🟢/🔴     │ │ [5秒倒计时确认]  │     │
│  └─────────┘ └──────────┘ └──────────┘ └──────────────────┘     │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  🟢 日亏损 0.3% | 🟢 连亏 0笔 | 🟢 行业 18% | 🟢 持仓 3/5 │   │
│  │  当前风控等级: 🟢 正常                                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─── 实时告警 ───────────────────────────────────────────┐     │
│  │  14:32 🟡 行业电子 28%→逼近25%上限                       │     │
│  │  14:15 🟢 自动买入 sh600030 @28.45                      │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                 │
│  ┌─── 操作日志 ───────────────────────────────────────────┐     │
│  │  14:30 老板 开启实盘                                     │     │
│  │  09:15 系统 日初复位                                     │     │
│  └────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
       ┌────────────┐   ┌────────────┐   ┌────────────┐
       │master_switch│   │risk_budget │   │audit.jsonl │
       │   .py       │   │   .py      │   │ (哈希链)    │
       └──────┬─────┘   └──────┬─────┘   └────────────┘
              │                │
              └────────┬───────┘
                       ▼
        ┌──────────────────────────────┐
        │        所有交易通道           │
        │                              │
        │  QMT快速通道 (passorder)     │
        │  审核通道   (POST Flask)     │
        │  live_trader (自动交易)      │
        │  paper_engine (模拟盘)       │
        │                              │
        │  下单前: if not can_buy():   │
        │           return  ← 1行搞定  │
        └──────────────────────────────┘
```

### 2.3 三级预警 — 完整规则

| 级别 | 颜色 | 触发条件 | 动作 | 恢复 |
|------|:--:|------|------|:--:|
| 正常 | 🟢 | 全部指标正常 | 正常交易 | - |
| 关注 | 🟡 | 日亏>2% / 连亏2笔 / 行业>20% / 仓位>70% / 波动率>3% | 钉钉提醒，**不限制交易** | 自动 |
| 警告 | 🟠 | 日亏>3.5% / 连亏3笔 / 行业>23% / 仓位>85% | 暂停新开仓，**只允许减仓** | 人工确认 |
| 熔断 | 🔴 | 日亏>5% / 连亏5笔 / 行业>25% / 总闸关闭 / 手动紧急停止 | 撤所有挂单，**禁止一切买入** | 必须人工复位 |

**核心铁律**: 🟠橙和🔴红状态下，**卖出/减仓/止损单永不拦截**。

### 2.4 动态阈值增强

对齐铁律#14: 三色灯阈值随市场状态动态调整。

| 条件 | 牛市 | 震荡 | 熊市 |
|------|:--:|:--:|:--:|
| 🟡 日亏损 | >3% | >2% | >1.5% |
| 🟠 日亏损 | >5% | >3.5% | >2.5% |
| 🔴 日亏损 | >8% | >5% | >3.5% |
| 🟡 行业集中度 | >25% | >20% | >15% |

牛市放宽（机会多），熊市收紧（保护本金）。

---

## 三、接口设计

### 3.1 master_switch.py（精简版）

```python
"""master_switch.py — 全局总闸, 零依赖, 所有通道下单前唯一检查点"""
import json, os
from datetime import datetime

_STATE_FILE = r"D:\quant_framework\master_switch.json"

class MasterSwitch:
    def __init__(self):
        self.master_kill = False     # 总闸
        self.real_enabled = False    # 实盘 (默认关)
        self.sim_enabled = True      # 模拟盘 (默认开)
        self.emergency_stop = False  # 紧急停止
        self._load()

    def can_buy(self, channel="sim") -> bool:
        if self.emergency_stop or self.master_kill:
            return False
        if channel == "real":
            return self.real_enabled
        return self.sim_enabled

    def can_sell(self) -> bool:
        return True  # FIA 2024 铁律: 减仓永不拦截

    def emergency_halt(self, operator="老板", reason=""):
        self.emergency_stop = True
        self.master_kill = True
        self.real_enabled = False
        self._save()
        self._audit(operator, "EMERGENCY_HALT", reason)

    def enable_real(self, operator="老板", confirm_code=""):
        if confirm_code != self._confirm_code():
            return False, "确认码错误"
        self.real_enabled = True
        self.master_kill = False
        self.emergency_stop = False
        self._save()
        self._audit(operator, "REAL_ENABLED", "")
        return True, "实盘已开启"

    def get_risk_level(self, daily_loss, consecutive_loss, sector_pct,
                       position_pct, volatility, regime="sideways"):
        """三色灯判定"""
        thresholds = {
            "bull": {"yellow": (3,2,25,70,3), "orange": (5,3,28,85,4), "red": (8,5,30,95,5)},
            "sideways": {"yellow": (2,2,20,70,3), "orange": (3.5,3,23,85,4), "red": (5,5,25,95,5)},
            "bear": {"yellow": (1.5,2,15,70,2.5), "orange": (2.5,3,18,85,3.5), "red": (3.5,5,20,95,4.5)},
        }
        t = thresholds.get(regime, thresholds["sideways"])
        dl, cl, sp, pp, vol = abs(daily_loss), consecutive_loss, sector_pct, position_pct, volatility
        if dl>=t["red"][0] or cl>=t["red"][1] or sp>=t["red"][2]: return "🔴"
        if dl>=t["orange"][0] or cl>=t["orange"][1] or sp>=t["orange"][2]: return "🟠"
        if dl>=t["yellow"][0] or cl>=t["yellow"][1] or sp>=t["yellow"][2]: return "🟡"
        return "🟢"

    # 内部方法略 (同郑方案 v1.0 的 _load/_save/_audit/_confirm_code)
```

### 3.2 各通道接入 (每处1行)

```python
# QMT策略 (qmt_full_strategy.py 快速通道段)
from master_switch import MasterSwitch
_ms = MasterSwitch()
# ... 在 passorder 前:
if not _ms.can_buy("real"): continue  # ← 1行

# live_trader.py
from master_switch import MasterSwitch
_ms = MasterSwitch()
# ... 在 send_buy_order 前:
if not _ms.can_buy("real"): return  # ← 1行

# paper_engine.py
from master_switch import MasterSwitch
_ms = MasterSwitch()
# ... 在 place_order 前:
if side == "buy" and not _ms.can_buy("sim"): return  # ← 1行

# Flask审批 (/api/signal/approve)
from master_switch import MasterSwitch
_ms = MasterSwitch()
if not _ms.can_buy("sim"): return jsonify({"code":403, "error":"总闸关闭"})
```

---

## 四、实施优先级

| 优先级 | 内容 | 文件 | 时间 |
|:--:|------|------|:--:|
| 🔴 P0 | `master_switch.py` — 总闸控制器 | 新增 | 30min |
| 🔴 P0 | 接入 paper_engine + live_trader + QMT | 修改3处各1行 | 15min |
| 🔴 P0 | `/api/kill-switch/status` + `/toggle` API | app.py +30行 | 20min |
| 🟡 P1 | `/risk-panel` Web页面 | 新增模板 | 1h |
| 🟡 P1 | `risk_budget.py` — 风控预算引擎 | 新增 | 30min |
| 🟡 P1 | 钉钉告警接入 | app.py +5行 | 10min |
| 🟢 P2 | 动态阈值 (市场状态自适应) | master_switch.py | 20min |
| 🟢 P2 | audit.jsonl 哈希链 | master_switch.py | 15min |

---

## 五、三方案最终评分

| 维度 | 赵方案 v1.0 | 郑方案 v1.0 | 行业标准 | **终极 v2.0** |
|------|:--:|:--:|:--:|:--:|
| 完整性 | 7 | **9** | 10 | **9.5** |
| 落地性 | **9** | 6 | 4 | **9** |
| 可维护 | 7 | **9** | 6 | **9** |
| 安全性 | 7 | 8 | **10** | **9** |
| 个人适配 | 8 | 7 | 3 | **9** |
| 扩展性 | 6 | **9** | 9 | **8.5** |
| **综合** | **7.3** | **8.0** | **7.0** | **9.0** |

### 为什么终极方案最高

1. **架构** — 继承了郑方案的 FIA 五层 + can_buy/can_sell 干净接口
2. **分级** — 继承了郑方案的黄橙红三级 + 动态阈值增强
3. **落地** — 继承了赵方案的精确到文件+行号的接入指南
4. **安全** — 继承了行业标准的减仓放行 + 人工复位 + 哈希链审计
5. **个人适配** — 去掉了硬件 kill switch、跨交易所引擎等机构专属，精简到 4 个文件

---

## 六、与蓝图对齐

| 蓝图条款 | 终极方案 |
|------|------|
| 铁律#14 参数三层治理 | master_switch.json 第2层, trade_config 第1层基准 |
| 人工在回路 | 总闸默认关 / 确认码开实盘 / 熔断手动复位 |
| 单一真相源 | master_switch.json 所有通道读它 |
| 不跳模拟上实盘 | 总闸默认关 + 实盘单独开关 + 确认码 |
| 市场状态自适应 | 三色灯阈值随牛/熊动态调整 |

---

## 关联

- [[风控总闸设计方案_v1.0]] — 赵方案
- `D:\quant_framework\docs\风控操作台设计方案_v1.md` — 郑方案
- [[signal-id-idempotency]] — 同期规范
- [[architecture-upgrade-guide]] — 铁律
